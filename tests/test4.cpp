#include "../src/tools/math.hpp"
#include "../src/tools/mesh.hpp"
#include "../src/assembly.hpp"


using namespace std;
int main() {
  Vector sol = solve(, tuple<ld, ld> topLeft, tuple<ld, ld> bottomRight, ld precision, ld (*M)[2], pair<ld, ld> a, ld C5, function<ld (ld, ld)> f)
}
